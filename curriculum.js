(() => {
    const defaults = [
        { id: 1, subjectName: 'Mathematics', subjectCode: 'MATH-06', curriculumType: 'Core', className: 'Grade 6', section: 'A', academicYear: '2026 - 2027', teacherName: 'Mr. Ahmed Hassan', lessons: 40, weeklyPeriods: 5, totalHours: 40, status: 'Active', startDate: '2026-04-10', endDate: '2027-03-20', objectives: 'Develop mathematical reasoning, problem solving and numerical skills.', topics: 'Numbers, fractions, decimals, algebra, geometry and statistics.', notes: 'Regular assessment required.' },
        { id: 2, subjectName: 'English', subjectCode: 'ENG-07', curriculumType: 'Core', className: 'Grade 7', section: 'B', academicYear: '2026 - 2027', teacherName: 'Ms. Sara Ali', lessons: 38, weeklyPeriods: 5, totalHours: 38, status: 'Active', startDate: '2026-04-10', endDate: '2027-03-20', objectives: 'Improve reading, writing, speaking and comprehension skills.', topics: 'Grammar, reading comprehension, writing, literature and communication.', notes: 'Include project-based learning.' },
        { id: 3, subjectName: 'Science', subjectCode: 'SCI-08', curriculumType: 'Core', className: 'Grade 8', section: 'A', academicYear: '2026 - 2027', teacherName: 'Mr. John Thomas', lessons: 42, weeklyPeriods: 5, totalHours: 42, status: 'Active', startDate: '2026-04-10', endDate: '2027-03-20', objectives: 'Develop scientific knowledge and practical investigation skills.', topics: 'Matter, energy, living organisms, environment and physical science.', notes: 'Laboratory activities should be included.' }
    ];
    const fields = ['subjectName','subjectCode','curriculumType','className','section','academicYear','teacherName','lessons','weeklyPeriods','totalHours','status','startDate','endDate','objectives','topics','notes'];
    const escape = value => String(value || '').replace(/[&<>"']/g, character => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#039;' }[character]));
    const read = () => { try { return JSON.parse(localStorage.getItem('school_curriculum')) || defaults; } catch { return defaults; } };
    const save = records => localStorage.setItem('school_curriculum', JSON.stringify(records));
    const date = value => value ? new Date(`${value}T00:00:00`).toLocaleDateString('en-GB', { day:'2-digit', month:'short', year:'numeric' }) : '-';

    window.createCurriculumModule = function createCurriculumModule() {
        const sec = document.createElement('section'); sec.className = 'module curriculum-module'; sec.id = 'curriculumModule';
        sec.innerHTML = `<header class="cm-topbar"><div><h1>Curriculum Management</h1><p>Manage curriculum, subjects, lessons and academic planning</p></div><div class="cm-actions"><button class="cm-icon-btn" type="button" data-theme title="Theme">🌙</button><button class="cm-icon-btn" type="button" data-print-current title="Print">🖨️</button><div class="cm-user"><div class="cm-avatar">AD</div><div><strong>Administrator</strong><small>School Admin</small></div></div></div></header><div class="cm-cards"><div class="cm-card cm-stat"><div><p>Total Curriculum</p><h2 data-stat="curriculum">0</h2><span>Curriculum records</span></div><div class="cm-stat-icon cm-blue">📚</div></div><div class="cm-card cm-stat"><div><p>Subjects</p><h2 data-stat="subjects">0</h2><span>Subjects available</span></div><div class="cm-stat-icon cm-cyan">📖</div></div><div class="cm-card cm-stat"><div><p>Classes Covered</p><h2 data-stat="classes">0</h2><span>Academic classes</span></div><div class="cm-stat-icon cm-green">🏫</div></div><div class="cm-card cm-stat"><div><p>Active Plans</p><h2 data-stat="active">0</h2><span>Current curriculum</span></div><div class="cm-stat-icon cm-orange">✓</div></div></div><div class="cm-section-head"><div><h2>Curriculum</h2><p class="cm-subtitle">Curriculum subjects and academic planning records</p></div><div class="cm-toolbar"><div class="cm-search"><span>🔍</span><input type="search" data-search placeholder="Search curriculum..."></div><button class="cm-btn cm-btn-light" type="button" data-export>⬇ Export</button><button class="cm-btn cm-btn-primary" type="button" data-add>+ Add Curriculum</button></div></div><div class="cm-card cm-table-card"><div class="cm-table-wrap"><table><thead><tr><th>Subject</th><th>Code</th><th>Class</th><th>Section</th><th>Academic Year</th><th>Teacher</th><th>Lessons</th><th>Status</th><th>Actions</th></tr></thead><tbody data-table></tbody></table></div></div><div class="cm-modal" data-modal><div class="cm-modal-box"><div class="cm-modal-head"><div><h2 data-modal-title>Add Curriculum</h2><p class="cm-subtitle">Complete the curriculum information</p></div><button class="cm-close" type="button" data-close>×</button></div><form data-form><div class="cm-form-body"><div class="cm-form-section"><h3>📚 Curriculum Information</h3><div class="cm-form-grid"><label class="cm-form-group">Subject Name *<input name="subjectName" required placeholder="e.g. Mathematics"></label><label class="cm-form-group">Subject Code *<input name="subjectCode" required placeholder="e.g. MATH-06"></label><label class="cm-form-group">Curriculum Type<select name="curriculumType"><option>Core</option><option>Elective</option><option>Optional</option><option>Co-Curricular</option></select></label><label class="cm-form-group">Class *<select name="className" required><option value="">Select Class</option>${Array.from({length:12}, (_, i) => `<option>Grade ${i + 1}</option>`).join('')}</select></label><label class="cm-form-group">Section<select name="section"><option>A</option><option>B</option><option>C</option><option>D</option></select></label><label class="cm-form-group">Academic Year<input name="academicYear" value="2026 - 2027"></label></div></div><div class="cm-form-section"><h3>📝 Academic Planning</h3><div class="cm-form-grid"><label class="cm-form-group">Teacher<input name="teacherName" placeholder="Subject teacher"></label><label class="cm-form-group">Total Lessons<input type="number" name="lessons" min="0" value="40"></label><label class="cm-form-group">Weekly Periods<input type="number" name="weeklyPeriods" min="0" value="5"></label><label class="cm-form-group">Total Hours<input type="number" name="totalHours" min="0" value="40"></label><label class="cm-form-group">Status<select name="status"><option>Active</option><option>Draft</option><option>Inactive</option><option>Completed</option></select></label><label class="cm-form-group">Start Date<input type="date" name="startDate"></label><label class="cm-form-group">End Date<input type="date" name="endDate"></label><label class="cm-form-group full">Learning Objectives<textarea name="objectives" placeholder="Enter learning objectives..."></textarea></label><label class="cm-form-group full">Topics / Syllabus<textarea name="topics" placeholder="Enter major topics, units or syllabus content..."></textarea></label><label class="cm-form-group full">Additional Notes<textarea name="notes" placeholder="Additional curriculum notes..."></textarea></label></div></div></div><div class="cm-modal-foot"><button type="button" class="cm-btn cm-btn-light" data-cancel>Cancel</button><button type="submit" class="cm-btn cm-btn-primary">Save Curriculum</button></div></form></div></div><div class="cm-preview-modal" data-preview-modal><div class="cm-preview-box"><div class="cm-preview-toolbar"><h3>Curriculum Preview</h3><div class="cm-preview-actions"><button class="cm-print-btn" type="button" data-preview-print>🖨 Print</button><button class="cm-close-preview" type="button" data-preview-close>Close</button></div></div><div data-preview-content></div></div></div><div class="cm-print-area" data-print-area></div>`;
        let records = read(); let editingId = null; let previewId = null;
        const $ = selector => sec.querySelector(selector); const form = $('[data-form]'); const modal = $('[data-modal]');
        const render = () => { const query = $('[data-search]').value.toLowerCase().trim(); const filtered = records.filter(record => Object.values(record).join(' ').toLowerCase().includes(query)); $('[data-table]').innerHTML = filtered.length ? filtered.map(record => { const badge = record.status === 'Draft' ? 'cm-badge-orange' : record.status === 'Inactive' ? 'cm-badge-red' : 'cm-badge-green'; return `<tr><td><div class="cm-subject"><div class="cm-subject-icon">📚</div><div><strong>${escape(record.subjectName)}</strong><small>${escape(record.curriculumType)}</small></div></div></td><td><strong>${escape(record.subjectCode)}</strong></td><td>${escape(record.className)}</td><td>${escape(record.section)}</td><td>${escape(record.academicYear)}</td><td>${escape(record.teacherName || '-')}</td><td>${escape(record.lessons || '0')}</td><td><span class="cm-badge ${badge}">${escape(record.status)}</span></td><td><div class="cm-row-actions"><button class="cm-action-btn" type="button" data-action="preview" data-id="${record.id}" title="Preview">👁️</button><button class="cm-action-btn" type="button" data-action="edit" data-id="${record.id}" title="Edit">✏️</button><button class="cm-action-btn" type="button" data-action="delete" data-id="${record.id}" title="Delete">🗑️</button></div></td></tr>`; }).join('') : '<tr><td colspan="9"><div class="cm-empty"><div style="font-size:40px">📚</div><h3>No curriculum found</h3><p>Add a curriculum record or change your search.</p></div></td></tr>'; $('[data-stat="curriculum"]').textContent = records.length; $('[data-stat="subjects"]').textContent = new Set(records.map(record => record.subjectName)).size; $('[data-stat="classes"]').textContent = new Set(records.map(record => record.className)).size; $('[data-stat="active"]').textContent = records.filter(record => record.status === 'Active').length; };
        const openForm = record => { editingId = record?.id || null; $('[data-modal-title]').textContent = record ? 'Edit Curriculum' : 'Add Curriculum'; form.reset(); if (!record) { form.elements.academicYear.value = '2026 - 2027'; form.elements.lessons.value = 40; form.elements.weeklyPeriods.value = 5; form.elements.totalHours.value = 40; } else fields.forEach(field => { form.elements[field].value = record[field] || ''; }); modal.classList.add('show'); };
        const closeForm = () => { modal.classList.remove('show'); editingId = null; };
        const documentMarkup = record => `<div class="cm-print-page"><div class="cm-print-content"><div class="cm-print-header"><div class="cm-print-logo cm-blue" style="display:grid;place-items:center;font-size:25px;font-weight:800">S</div><div class="cm-print-header-text"><h1>SMART SCHOOL</h1><h2>Curriculum Information Record</h2><p>Excellence · Character · Knowledge</p></div></div><div class="cm-print-title"><h2>CURRICULUM PROFILE</h2></div><div class="cm-summary"><div class="cm-summary-box"><strong>${escape(record.lessons)}</strong><span>Total Lessons</span></div><div class="cm-summary-box"><strong>${escape(record.weeklyPeriods)}</strong><span>Weekly Periods</span></div><div class="cm-summary-box"><strong>${escape(record.totalHours)}</strong><span>Total Hours</span></div><div class="cm-summary-box"><strong>${escape(record.status)}</strong><span>Status</span></div></div><div class="cm-print-section">CURRICULUM INFORMATION</div><table class="cm-info-table"><tr><th>Subject</th><td>${escape(record.subjectName)}</td><th>Subject Code</th><td>${escape(record.subjectCode)}</td></tr><tr><th>Curriculum Type</th><td>${escape(record.curriculumType)}</td><th>Academic Year</th><td>${escape(record.academicYear)}</td></tr><tr><th>Class</th><td>${escape(record.className)}</td><th>Section</th><td>${escape(record.section)}</td></tr><tr><th>Teacher</th><td>${escape(record.teacherName)}</td><th>Start Date</th><td>${date(record.startDate)}</td></tr><tr><th>End Date</th><td>${date(record.endDate)}</td><th>Status</th><td>${escape(record.status)}</td></tr></table>${[['LEARNING OBJECTIVES','objectives'],['TOPICS / SYLLABUS','topics'],['ADDITIONAL NOTES','notes']].map(([title, field]) => `<div class="cm-print-section">${title}</div><table class="cm-info-table"><tr><td style="height:65px">${escape(record[field] || `No ${field.toLowerCase()} provided.`)}</td></tr></table>`).join('')}<div class="cm-print-section">OFFICIAL SIGNATORIES</div><div class="cm-signatures"><div class="cm-signature">Curriculum Coordinator</div><div class="cm-signature">Academic Head / Principal</div><div class="cm-signature">Head of School</div></div></div><div class="cm-print-footer">This document is an official curriculum record generated by the School Management System.</div></div>`;
        const showPreview = record => { previewId = record.id; $('[data-preview-content]').innerHTML = documentMarkup(record); $('[data-preview-modal]').classList.add('show'); };
        const printRecord = record => { $('[data-print-area]').innerHTML = documentMarkup(record); window.print(); };
        $('[data-add]').addEventListener('click', () => openForm()); $('[data-search]').addEventListener('input', render); $('[data-close]').addEventListener('click', closeForm); $('[data-cancel]').addEventListener('click', closeForm); modal.addEventListener('click', event => { if (event.target === modal) closeForm(); }); $('[data-preview-close]').addEventListener('click', () => $('[data-preview-modal]').classList.remove('show')); $('[data-preview-modal]').addEventListener('click', event => { if (event.target === $('[data-preview-modal]')) $('[data-preview-modal]').classList.remove('show'); }); $('[data-preview-print]').addEventListener('click', () => { const record = records.find(item => item.id === previewId); if (record) printRecord(record); }); $('[data-print-current]').addEventListener('click', () => { if (records[0]) showPreview(records[0]); }); $('[data-theme]').addEventListener('click', () => sec.classList.toggle('cm-dark'));
        $('[data-table]').addEventListener('click', event => { const button = event.target.closest('[data-action]'); if (!button) return; const record = records.find(item => String(item.id) === button.dataset.id); if (!record) return; if (button.dataset.action === 'preview') showPreview(record); if (button.dataset.action === 'edit') openForm(record); if (button.dataset.action === 'delete' && confirm(`Delete ${record.subjectName} curriculum?`)) { records = records.filter(item => item.id !== record.id); save(records); render(); } });
        form.addEventListener('submit', event => { event.preventDefault(); const data = Object.fromEntries(new FormData(form).entries()); data.id = editingId || Date.now(); if (editingId) records[records.findIndex(item => item.id === editingId)] = data; else records.push(data); save(records); render(); closeForm(); });
        $('[data-export]').addEventListener('click', () => { if (!records.length) return alert('No curriculum records to export.'); const headers = ['Subject','Subject Code','Curriculum Type','Class','Section','Academic Year','Teacher','Lessons','Weekly Periods','Total Hours','Status','Start Date','End Date','Objectives','Topics','Notes']; const rows = records.map(record => fields.map(field => record[field])); const csv = [headers, ...rows].map(row => row.map(value => `"${String(value || '').replace(/"/g, '""')}"`).join(',')).join('\n'); const link = document.createElement('a'); link.href = URL.createObjectURL(new Blob([csv], { type: 'text/csv' })); link.download = 'curriculum.csv'; link.click(); URL.revokeObjectURL(link.href); });
        render(); return sec;
    };
})();